# Base URL of the .NET Web API. Not used yet — the app currently runs
# entirely on mock/in-memory data (see src/services/*). Wire this up when
# the HttpProviderService / HttpProjectService implementations are added.
NEXT_PUBLIC_API_BASE_URL=https://localhost:5001
